# API Contract

This document provides a basic contract for the API endpoints and communication between the frontend and the backend.

## Endpoints

### 1. Product API
- `GET /api/products` : Retrieve a list of products.
- `GET /api/products/{id}` : Retrieve a single product by ID.
