<Window x:Class="MyShop.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyShop.Client"
        xmlns:vm="clr-namespace:MyShop.Client.ViewModels"
        xmlns:views="clr-namespace:MyShop.Client.Views"
        mc:Ignorable="d"
        Title="MyShop" Height="600" Width="1200" MinWidth="650" >

    <Window.Resources>
        <!-- DataTemplate: "Bản đồ" để WPF biết View nào đi với ViewModel nào -->
        <DataTemplate DataType="{x:Type vm:AuthViewModel}">
            <views:AuthView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:MainViewModel}">
            <!-- Giả sử bạn có MainView, nếu chưa có hãy thay bằng một TextBlock để test -->
            <views:MainView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <!-- ContentControl sẽ tự động hiển thị View tương ứng với DataContext -->
        <ContentControl Content="{Binding}" />
    </Grid>
</Window>
