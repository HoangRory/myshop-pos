<UserControl x:Class="MyShop.Client.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:MyShop.Client.Helpers"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" Background="#F4F7FC"
             d:DesignHeight="1000" d:DesignWidth="1200">

    <UserControl.Resources>
        <helpers:RevenueToHeightConverter x:Key="RevenueToHeightConverter"/>
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="15" Direction="270" ShadowDepth="2" Opacity="0.1"/>

        <DataTemplate x:Key="ProductItemTemplate">
            <Grid Margin="0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Width="45" Height="45" CornerRadius="8" Background="#FFF" BorderBrush="#E6E9EC" BorderThickness="1">
                    <Grid>
                        <Image Source="{Binding Thumbnail}" Stretch="UniformToFill" />
                        <TextBlock Text="{Binding Sku}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#6B6F76" FontSize="8" FontWeight="Bold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Thumbnail}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>
                <StackPanel Grid.Column="1" Margin="12,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Name}" FontWeight="Medium" TextTrimming="CharacterEllipsis" FontSize="13" Foreground="#2D3436"/>
                    <TextBlock Text="{Binding SalePrice, StringFormat={}{0:N0}đ}" FontSize="11" Foreground="#6B6F76"/>
                </StackPanel>
                <Border Grid.Column="2" VerticalAlignment="Center" Padding="8,2" CornerRadius="10" Background="#FEE2E2">
                    <TextBlock Text="{Binding StockCount, StringFormat=Kho: {0}}" FontSize="10" FontWeight="Bold" Foreground="#991B1B"/>
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="OrderStatusTemplate">
            <Grid HorizontalAlignment="Center">
                <Border x:Name="StatusBadge" Background="#E5E7EB" CornerRadius="5" Padding="8,3">
                    <TextBlock x:Name="StatusTxt" Text="Chờ xử lý" Foreground="#374151" FontSize="11"/>
                </Border>
            </Grid>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="1">
                    <Setter TargetName="StatusBadge" Property="Background" Value="#D1FAE5"/>
                    <Setter TargetName="StatusTxt" Property="Text" Value="Đã thanh toán"/>
                    <Setter TargetName="StatusTxt" Property="Foreground" Value="#065F46"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="2">
                    <Setter TargetName="StatusBadge" Property="Background" Value="#FEE2E2"/>
                    <Setter TargetName="StatusTxt" Property="Text" Value="Đã hủy"/>
                    <Setter TargetName="StatusTxt" Property="Foreground" Value="#991B1B"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <Style x:Key="LoadingOverlayStyle" TargetType="Border">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Setter Property="IsHitTestVisible" Value="True"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsLoaded}" Value="False">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                    <Setter Property="IsHitTestVisible" Value="False"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Hệ thống quản lý MyShop" FontSize="24" FontWeight="Bold" Foreground="#2D3436"/>
                <TextBlock Text="Phân tích hiệu quả kinh doanh thực tế" Foreground="#636E72"/>
            </StackPanel>

            <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,20">
                <Border Background="White" CornerRadius="10" Padding="15" Margin="5" Effect="{StaticResource ShadowEffect}">
                    <StackPanel>
                        <TextBlock Text="Tổng Sản Phẩm" Foreground="#ADBAC7"/>
                        <TextBlock Text="{Binding DashboardData.TotalProducts}" FontSize="28" FontWeight="Bold" Margin="0,5" Foreground="#2F6F9F"/>
                    </StackPanel>
                </Border>
                <Border Background="White" CornerRadius="10" Padding="15" Margin="5" Effect="{StaticResource ShadowEffect}">
                    <StackPanel>
                        <TextBlock Text="Đơn Hàng Hôm Nay" Foreground="#ADBAC7"/>
                        <TextBlock Text="{Binding DashboardData.TotalOrdersToday}" FontSize="28" FontWeight="Bold" Margin="0,5" Foreground="#3498DB"/>
                    </StackPanel>
                </Border>
                <Border Background="White" CornerRadius="10" Padding="15" Margin="5" Effect="{StaticResource ShadowEffect}">
                    <StackPanel>
                        <TextBlock Text="Doanh Thu Hôm Nay" Foreground="#ADBAC7"/>
                        <TextBlock Text="{Binding DashboardData.TotalRevenueToday, StringFormat={}{0:N0}đ}" FontSize="28" FontWeight="Bold" Margin="0,5" Foreground="#27AE60"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFF5F5" CornerRadius="10" Padding="15" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Cảnh Báo Hết Hàng" Foreground="#C53030"/>
                        <TextBlock Text="{Binding DashboardData.LowStockProducts.Count}" FontSize="28" FontWeight="Bold" Margin="0,5" Foreground="#C53030"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Border Grid.Row="2" Background="White" CornerRadius="10" Margin="5,0,5,20" Padding="20" Effect="{StaticResource ShadowEffect}">
                <StackPanel>
                    <TextBlock Text="Doanh thu theo ngày trong tháng" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20" Foreground="#34495E"/>
                    <Grid Height="300">
                        <lvc:CartesianChart Series="{Binding RevenueSeries}" Hoverable="True">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Ngày" Labels="{Binding Labels}"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="VNĐ" LabelFormatter="{Binding Formatter}" MinValue="0"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </StackPanel>
            </Border>

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="5" Padding="15" Effect="{StaticResource ShadowEffect}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Đơn hàng gần đây" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <DataGrid ItemsSource="{Binding DashboardData.RecentOrders}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="None" Background="Transparent" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding OrderId}" Width="Auto"/>
                                <DataGridTextColumn Header="Tổng Tiền" Binding="{Binding FinalTotal, StringFormat={}{0:N0}đ}" Width="*"/>
                                <DataGridTemplateColumn Header="Trạng Thái" CellTemplate="{StaticResource OrderStatusTemplate}" Width="Auto"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>

                <Border Grid.Column="1" Background="White" CornerRadius="10" Margin="5" Padding="15" Effect="{StaticResource ShadowEffect}">
                    <StackPanel>
                        <TextBlock Text="Top Bán Chạy" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <ItemsControl ItemsSource="{Binding DashboardData.BestSellingProducts}" ItemTemplate="{StaticResource ProductItemTemplate}"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="White" CornerRadius="10" Margin="5" Padding="15" Effect="{StaticResource ShadowEffect}">
                    <StackPanel>
                        <TextBlock Text="Cần Nhập Hàng" FontSize="16" FontWeight="SemiBold" Foreground="#C53030" Margin="0,0,0,10"/>
                        <ItemsControl ItemsSource="{Binding DashboardData.LowStockProducts}" ItemTemplate="{StaticResource ProductItemTemplate}"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.RowSpan="4" Background="#B2FFFFFF" Style="{StaticResource LoadingOverlayStyle}" CornerRadius="10">
                <StackPanel VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="6" Foreground="#2F6F9F"/>
                    <TextBlock Text="Đang xử lý dữ liệu LuciferCore..." HorizontalAlignment="Center" Margin="15" FontWeight="Medium"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>