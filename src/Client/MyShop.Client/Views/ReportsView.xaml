<UserControl x:Class="MyShop.Client.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:MyShop.Client.ViewModels"
             xmlns:helpers="clr-namespace:MyShop.Client.Helpers"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280"
             d:DataContext="{d:DesignInstance Type=viewmodels:ReportsViewModel}"
             Padding="14">

    <UserControl.Resources>
        <!-- Quan trọng: Đăng ký Converter để dùng cho RadioButtons -->
        <helpers:IntToBoolConverter x:Key="IntToBoolConverter" />

        <!-- Resource colors -->
        <SolidColorBrush x:Key="AppBackground" Color="#F6F7F8" />
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF" />
        <SolidColorBrush x:Key="CardBorderBrush" Color="#E6E9EC" />
        <SolidColorBrush x:Key="MutedBrush" Color="#6B6F76" />
        <SolidColorBrush x:Key="AccentBrush" Color="#2F6F9F" />

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}" />
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Margin" Value="0,0,0,16" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="24,10" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="GroupButtonStyle" TargetType="RadioButton" BasedOn="{StaticResource {x:Type ToggleButton}}">
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" LastChildFill="True">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                <TextBlock Text="Báo cáo &amp; Thống kê" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" />
                <TextBlock Text=" • Phân tích dữ liệu kinh doanh" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="14"/>
            </StackPanel>
        </DockPanel>

        <!-- Filters Section - FIX LỖI CHILD HERE -->
        <Border Style="{StaticResource CardStyle}" Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <!-- Nhóm 1: Date Filter -->
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,24,0">
                        <TextBlock Text="Từ ngày" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,5" FontSize="12"/>
                        <DatePicker SelectedDate="{Binding FromDate}" Width="160" Height="32" VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,30,0">
                        <TextBlock Text="Đến ngày" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,5" FontSize="12"/>
                        <DatePicker SelectedDate="{Binding ToDate}" Width="160" Height="32" VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <Button Content="Cập nhật dữ liệu" 
                            Command="{Binding RefreshCommand}" 
                            Style="{StaticResource PrimaryButton}" 
                            VerticalAlignment="Bottom"/>
                </StackPanel>

                <!-- Nhóm 2: Group Type Selection -->
                <StackPanel Orientation="Horizontal" Margin="40,0,0,0" VerticalAlignment="Bottom">
                    <TextBlock Text="Xem theo:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource MutedBrush}"/>

                    <RadioButton Content="Ngày" Style="{StaticResource GroupButtonStyle}" 
                                 IsChecked="{Binding SelectedGroupType, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}"/>

                    <RadioButton Content="Tuần" Style="{StaticResource GroupButtonStyle}" 
                                 IsChecked="{Binding SelectedGroupType, Converter={StaticResource IntToBoolConverter}, ConverterParameter=2}"/>

                    <RadioButton Content="Tháng" Style="{StaticResource GroupButtonStyle}" 
                                 IsChecked="{Binding SelectedGroupType, Converter={StaticResource IntToBoolConverter}, ConverterParameter=3}"/>

                    <RadioButton Content="Năm" Style="{StaticResource GroupButtonStyle}" 
                                 IsChecked="{Binding SelectedGroupType, Converter={StaticResource IntToBoolConverter}, ConverterParameter=4}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Charts Content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,10,0">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Sản phẩm đã bán theo thời gian" FontSize="16" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,25"/>
                        <Grid Height="300">
                            <lvc:CartesianChart Series="{Binding ProductSeries}" LegendLocation="Right">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Title="Thời gian" Labels="{Binding Labels}" Separator="{x:Static lvc:DefaultAxes.CleanSeparator}"/>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Title="Số lượng" MinValue="0"/>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Thống kê Doanh thu &amp; Lợi nhuận" FontSize="16" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,25"/>
                        <Grid Height="300">
                            <lvc:CartesianChart Series="{Binding RevenueSeries}" LegendLocation="Bottom">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Title="Thời gian" Labels="{Binding Labels}" Separator="{x:Static lvc:DefaultAxes.CleanSeparator}"/>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Title="Số tiền" LabelFormatter="{Binding Formatter}" MinValue="0"/>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3" Background="#AAFFFFFF">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoaded}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <StackPanel VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="250" Height="6" Foreground="{StaticResource AccentBrush}"/>
                <TextBlock Text="Đang tổng hợp báo cáo..." HorizontalAlignment="Center" Margin="0,15,0,0" FontWeight="Medium"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>