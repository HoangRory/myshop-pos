<UserControl x:Class="MyShop.Client.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:MyShop.Client.Views"
             xmlns:vm="clr-namespace:MyShop.Client.ViewModels"
             xmlns:local="clr-namespace:MyShop.Client"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProductViewModel}">
            <views:ProductsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:OrdersViewModel}">
            <views:OrdersView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ReportsViewModel}">
            <views:ReportsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!-- Sidebar -->
        <StackPanel Grid.Column="0" Background="#2D2D30" VerticalAlignment="Stretch">
            <TextBlock Text="Menu" Foreground="White" FontWeight="Bold" FontSize="18" Margin="16,24,0,24" />
            <Button Content="Dashboard" Margin="8,4" Height="40" Command="{Binding NavigateDashboardCommand}" />
            <Button Content="Products" Margin="8,4" Height="40" Command="{Binding NavigateProductsCommand}" />
            <Button Content="Orders" Margin="8,4" Height="40" Command="{Binding NavigateOrdersCommand}" />
            <Button Content="Reports" Margin="8,4" Height="40" Command="{Binding NavigateReportsCommand}" />
            <Button Content="Settings" Margin="8,4" Height="40" Command="{Binding NavigateSettingsCommand}" />
        </StackPanel>
        <!-- Main Content -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}" />
    </Grid>
</UserControl>
